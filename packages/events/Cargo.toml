[package]
name = "events"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.44", default-features = false, features = ["serde"] }
engine = { path = "../engine" }
serde = { version = "1.0.228", features = ["derive"] }
uuid = { version = "1.23.1", default-features = false, features = ["serde"] }
